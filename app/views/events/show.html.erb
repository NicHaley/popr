<%= form_for([@event, @event.commitments.build], url: user_commitments_path(user_id: current_user.id, event_id: params[:id])) do |f| %>
	<div class="field">
	  <%= f.label :party_size %><br />
	  <%= f.number_field :party_size %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>