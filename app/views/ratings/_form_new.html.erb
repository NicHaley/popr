<%= form_for([@user, @rating], :remote => true) do |f| %>

	<p><strong>Leave a Review</strong></p>

		<div class="field starRating">
				<% if @rating.errors.any? %>
				<div id="error_explanation">
				  <h2><%= pluralize(@rating.errors.count, "error") %> prohibited this review from being saved:</h2>

				  <ul>
				    <% @rating.errors.full_messages.each do |message| %>
				    <li><%= message %></li>
				    <% end %>
				  </ul>
				</div>
				<% end %>		
		<div>
			<div>
				<div>
					<div>
						<%= f.radio_button :user_score, 1, id: "rating1" %>
						<label for="rating1"><span>1</span></label>
					</div>
					<%= f.radio_button :user_score, 2, id: "rating2" %>
					<label for="rating2"><span>2</span></label>
				</div>
				<%= f.radio_button :user_score, 3, id: "rating3" %>
				<label for="rating3"><span>3</span></label>
			</div>
			<%= f.radio_button :user_score, 4, id: "rating4" %>
			<label for="rating4"><span>4</span></label>
		</div>
		<%= f.radio_button :user_score, 5, id: "rating5" %>
		<label for="rating5"><span>5</span></label>
	</div><br /><br />
	<div class="field">
		<%= f.text_area :review, :size => "35x3", maxlength: 150, class: "message" %>
	</div>
	<div class="countdown"></div>
	<div class="field">
		<%= f.hidden_field :rt_id, value: "" %>
		<%= f.hidden_field :actors, value: "" %>
		<%= f.hidden_field :directors, value: "" %>
		<%= f.hidden_field :genres, value: "" %>
	</div> 
	<%= f.submit "Submit Review", class: "button expand", id: "rev-button" %>
<% end %>