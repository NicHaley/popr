<h1>User List</h1>

<div>
	<% @users.each do |user| %>
		<p>
			<%= user.first_name %> <%= user.last_name %>
			<%= link_to "Add Friend", friendships_path(friend_id: user.id), method: :post %>
		</p>
	<% end %>
</div>


